class Paper {
    constructor(x,y,radius) {
        var options = {
            isStatic: false,
            restitution:0.3,
            friction : 0.5,
            density  : 1.2
        }

    this.body = Matter.Bodies.Circle(56,46,55,);
    this.width = width;
    this.height =  height;
    this.image = loadImage
    World.add(world,this.body)
    }
display() {

    push()  

    imageMode(LEFT);
    pop();
    }
}